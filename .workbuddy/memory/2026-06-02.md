# 2026-06-02 工作日志

## Header 液态玻璃样式审查
- 完成 Header.astro 液态玻璃逻辑的全面代码审查
- 报告保存至 `lib/reports/HEADER_LIQUID_GLASS_REVIEW.md`
- 共发现 10 项问题：2 个 Blocker、4 个 Suggestion、4 个 Nit
- 核心问题：blur(20px) 全组件硬编码（7处）、缺少 saturate()、玻璃属性块重复（7处复制粘贴）
- 涉及文件：Header.astro、colors.css、dark.css、header.css、MusicPlayer.svelte、404.astro、garden.css
- 核查后发现 9/10 项已通过重构修复（glass-surface 统一类、blur token 化、saturate 增强等）

## Header 液态玻璃审查核查
- 逐条核实 10 项发现的实际修复状态，9 项已修，仅 border-width 语义保留

## 移动端底部元素布局审查
- 审查了音乐播放器、底部导航 Tab、回到顶部按钮三个固定元素的定位逻辑
- 发现三个元素的 `bottom` 值不一致（16px vs 20px）、FAB 按钮 `min-width: 56px` 未被覆盖
- 报告保存至 `lib/reports/LAYOUT_POSITIONING_UNIFICATION.md`

## 页面元素外部排版统一化方案
- 联网调研 Ant Design Seed→Map→Alias→Component 四层 Token 架构及 W3C DTCG 标准
- 设计了适用于本项目的四层布局 Token 体系（Seed 4 个原始值 → Map 自动推导 → Alias 语义绑定 → Component 引用）
- 核心创新：以 `--layout-base-unit: 11` (44px) 为基准，所有元素尺寸通过 ±n 偏移派生，改一个值全体系缩放
- 可行性评估：技术上零风险（CSS 自定义属性 calc 推导链），共涉及 3 文件 ~15 处替换
- 开放问题：底部导航 bottom 统一到 16px 后的视觉微调、播放器去 scale hack 的内部排版调整

## ToC 目录定位修复
- 桌面端：删除 TableOfContents.astro `.toc-sidebar` 的三重 sticky（position/top/max-height），由 SplitLayout `.split-sidebar` 统一管理
- 桌面端：`SplitLayout.astro` `.split-sidebar` 的 `top` 从硬编码 `5.5rem` 改为 `calc(var(--header-height) + var(--header-padding-y) + var(--blog-space-4))` (66px + 16px = 82px)
- 桌面端：`max-height` 同步 token 化：`calc(100vh - var(--header-height) - var(--header-padding-y) - var(--blog-space-4) - var(--blog-space-16))`
- 移动端：`SplitLayout.astro` `.split-sidebar` `top` 从 `60px`(嵌进 header 6px) 改为 `calc(var(--header-height) + var(--header-padding-y) + var(--blog-space-2))` (66px + 8px = 74px)
- 移动端：`TableOfContents.astro` `.toc-mobile` `top` 从 `64px` 改为 `calc(var(--header-height) + var(--header-padding-y) + var(--blog-space-3))` (66px + 12px = 78px)
- 修复 Bug：移动端 `.toc-mobile` 内 `position: relative` 覆盖了 `position: sticky`，导致 ToC 不粘性——已删除

## ToC 下拉菜单液态玻璃效果修复
- 移动端 `.toc-nav-mobile` 背景从不透明 surface 切回 liquid glass，并移除有色外边框
- 根因：`.toc-nav-mobile` 是 `.toc-mobile`（details）的子元素，嵌套 backdrop-filter 导致内外层模糊叠加、效果不一致
- 解法：重构 DOM，`.toc-nav-mobile` 提出为 `.toc-mobile-wrapper` 的直接子元素，与 `<details>` 平级，各自独立 backdrop-filter
- JS 同步 `<details open>` → `.open` class 控制下拉显隐
- 下拉菜单链接查询从 `mobileToc.querySelectorAll` 改为 `wrapper.querySelectorAll`
- 追加点击外部关闭功能

## 移动端 Header Logo 胶囊微调
- 左 padding: 6px → 8px (`--blog-space-2`)
- 右 padding: 20px → 16px (`--blog-space-4`)
- 字号: body-large (18px) → title-medium (16px)
- 高度: 40px (`--layout-capsule-height`) → 44px (`--blog-space-11`)，与 ToC 胶囊对齐

## 胶囊高度-字号比例 Token 化
- 在 `layout.css` 新增 `--pill-*` 三层 scale（sm=36px, md=40px, lg=44px），每层配对高度 + 字号 + 水平内边距
- 设计基准：`--pill-text-ratio: 0.32`（字号 ≈ 胶囊高度的 32%）
- sm: label-small (11px, 31%), md: label-medium (12px, 30%), lg: title-small (14px, 32%)
- 接入范围：
  - Header.astro：桌面三件套 height/width/min-width、移动端 logo height、settings drawer 按钮
  - TableOfContents.astro：移动端 `.toc-toggle` 的 height + font-size
  - MusicPlayer.svelte：`.is-capsule` 的 height
  - shape.css：`--player-capsule-radius` 的 calc 基准
- 全网搜索确认 `--layout-capsule-height` 仅剩 2 处引用：token 定义自身 + `--pill-h-md` 派生

## Content 文件术语统一（HIG）
- 重写 `apps/garden/src/content` 下所有文件，将「苹果设计 / Apple Design」统一为「HIG」
- 修改文件：
  - `blog/apple-meets-material3-zh.md`：标题「苹果设计遇上…」→「HIG 遇上…」
  - `blog/apple-meets-material3.md`：标题 + 正文首段统一为「Apple HIG」
  - `blog/astro-content-first-framework.md`：内部链接显示文字更新为「HIG Meets Material 3」
  - `blog/building-token-design-system.md`：同上
- `about/zh.md`、`about/en.md`、`config.ts` 已符合规范，无需修改

## Content 文件全面重写（根据项目现状）
- 根据项目当前实际状态，重写了 `apps/garden/src/content` 下全部 6 个 md 文件
- 重写文件：
  - `about/en.md`：更新技术栈（Astro 5.7、Svelte 5、@m3e/framework 包、Prism），重写 Design Philosophy 和 Colophon 为表格形式
  - `about/zh.md`：同上，中文版，增加分层表格
  - `blog/apple-meets-material3.md`：重写正文，新增 Token 架构说明、backdrop-filter 具体数值、动效 Token 说明，修正内部链接文字
  - `blog/apple-meets-material3-zh.md`：同上，中文版同步更新
  - `blog/astro-content-first-framework.md`：重写，新增 Svelte 5 Islands 说明、Prism 构建时分词、具体 KB 数值、构建流水线 ASCII 图
  - `blog/building-token-design-system.md`：重写，新增 Layout Token（--layout-base-unit）和 Pill Token（--pill-*）说明、@m3e/framework 包引用、具体 CSS 代码示例
- 全文统一使用「HIG」表述，无「苹果设计 / Apple Design」

## 全局 background-color transition 缺失审计与修复
- 用户反馈 HeroLayout.astro 深浅色切换时与其他部分不同步
- 根因：`body` 有 `transition: background-color ...`，但 CSS `transition` **不继承**，所有显式设置 `background-color: var(--blog-color-...)` 的子元素若自身无 `transition`，主题切换时均瞬间变色
- 使用 Agent（Explore）对 `packages/framework/src/styles/` 下所有 CSS 和 `components/` 下所有 `.astro`/`.svelte` 文件做了全面审计
- 共发现 18 处缺失 `transition: background-color`，已全部修复：

### CSS 文件（12 处）
- `buttons.css`：`.btn` 基类 `transition` 补全 `background-color, color, border-color`（通过 `var(--blog-transition-color)`）
- `global.css`：`.surface`、`.surface-variant`、`.surface-raised`、`.badge` 添加 `transition: var(--blog-transition-color)`
- `garden.css`：`.post-stage--*`（3 个阶段）、`.post-domain-badge`、`..wiki-popover` 添加 `transition`

### .astro / .svelte 文件（6 处）
- `HeroLayout.astro`：`.hero-layout-hero` 添加 `transition`
- `BlogPostLayout.astro`：`:not(pre) > code` 添加 `transition`
- `MDBadge.astro`：`.md-badge` 基类添加 `transition`
- `Header.astro`：`.glass-surface, .mobile-nav` 和 `.logo-dot` 补全 `transition`
- `MonetSwitcher.astro`：`.monet-dropdown` 添加 `transition`
- `TableOfContents.astro`：`.toc-mobile`（基础 + 响应式）+ `.toc-nav-mobile` 添加 `transition`
- `MDSnackbar.svelte`：`.md-snackbar` `transition` 补全 `background-color`
- `MusicPlayer.svelte`：`.drawer` 添加 `transition`

## 主题切换「开关灯」闪光转场
- 新增 `#theme-flash` 全屏遮罩（BaseLayout.astro）实现主题切换的闪光效果
- 深→浅模式：黑色遮罩瞬间覆盖 → 溶解露出新主题
- 浅→深模式：白色遮罩瞬间覆盖 → 溶解露出新主题
- 方向逻辑：目标主题为 light → 黑遮罩；目标为 dark → 白遮罩
- 修改文件：
  - `BaseLayout.astro`：新增 `#theme-flash` 元素 + CSS（.flash-enter/.flash-exit）+ `flashTheme()` JS + `window.__flashTheme` 全局导出
  - `ThemeToggle.astro`：`setThemeMode()` 在改属性前调用 `__flashTheme(resolvedTheme)`，并延迟一帧改属性以确保遮罩先覆盖
  - 初始加载跳过闪光（`resolvedTheme === currentTheme` 检测）
  - 快速连续切换自动跳过（遮罩动画中时直接 return）
  - 系统 `prefers-color-scheme` 变化同样触发闪光

## ThemeFlash 转场效果修复
- 用户反馈转场效果不明显。全盘排查后修复：
  - `is:global` — 消除 Astro scoped 样式隔离风险
  - `getBoundingClientRect()` — 强制同步渲染，确保浏览器在撤掉 enter 前已绘制遮罩（否则两阶段被合并到单帧）
  - 80ms 可见保持 — double rAF 仅 ~33ms，人眼无法感知；改为 80ms delay 让闪光清晰可见
  - `pointer-events: auto` — 闪光期间阻断点击
  - `z-index: 2147483647` — 保证覆盖所有元素
  - `will-change: opacity` — 提示浏览器 GPU 加速
- ThemeToggle.astro 调用链无需改动

## 页面加载蓝色闪烁修复
- 根因：`colors.css` `:root` 默认色（hue 230 蓝色）与默认 Monet 预设 "monet"（seed #6750A4 紫色）不一致，且 `<html>` 缺少初始 `data-monet` 属性
- 时序：Astro 注入 main.css（蓝 :root）→ MonetCSS 内联 style（紫 [data-monet]）→ 内联脚本设 data-monet → 先蓝后紫跳变
- 修复（3 处）：
  - `BaseLayout.astro:47`：`<html>` 添加 `data-monet="monet--tonalSpot"`，让 Monet CSS 规则从第一帧生效
  - `BaseLayout.astro:83`：`<meta theme-color>` 从 `#3B5CF6`(蓝) 改为 `#65558f`(紫，默认 Monet 主色)
  - `colors.css:9-20`：`:root` ref 色对齐 Monet 默认预设（primary 257/25%/45%, secondary 267/12%/41%, tertiary 346/25%/41%）
  - `monet.config.js:6`：`defaultSeed` 从 `#3B5CF6` 改为 `#6750A4`（与 presets[0] 保持一致）

## 色彩/字体跳动全面排查与修复
- 评估了所有首载跳变风险，产出 6 项发现：Monet(✅已修)、Dark/Light(🟡低)、body transition(🟡低)、Google Fonts CSSOM阻塞(🔴严重)、Web Font FOUT(🔴严重)、daisyUI冲突(💭微)
- 修复（4 处）：
  - `main.css:1`：移除 Google Fonts `@import`（阻塞整个 CSSOM 的元凶）
  - `BaseLayout.astro:48-63`：Google Fonts 改为 `<link media="print" onload>` 非阻塞加载 + `dns-prefetch` + `preconnect`；`<html>` 默认值 `data-theme="light"`
  - `reset.css:38`：`body` 新增 `font-size-adjust: 0.535`（Google Sans Flex x-height），字体切换时减少布局偏移
  - `BaseLayout.astro:13`：移除未使用的 `monetConfig` import
- 效果：CSS 不再被 Google Fonts 阻塞 → FCP 大幅提前；字体 Fallback→Web Font 切换时视觉跳动最小化

## index/about 页面瘦入口重构
- 将 index.astro 和 about.astro 重构为瘦入口模式：仅负责数据获取 + 委托渲染
- 新增 `packages/framework/src/pages/HomePage.astro`（151 行，从 index.astro 提取 Hero editorial section + Recent Posts）
- 新增 `packages/framework/src/pages/AboutPage.astro`（287 行，从 about.astro 提取 bio 内容 + M3 组件 showcase）
- 瘦入口 index.astro（139→26 行）：`import HomePage → getCollection → <HomePage props />`
- 瘦入口 about.astro（275→23 行）：`import AboutPage → getEntry + render → <AboutPage props />`
- 页面组件通过 props 接收 siteConfig（name/tagline/description/copyright/music）+ 内容数据，消除对 app 层 config 的直接依赖
- 导入路径：页面组件内使用相对路径 `../layouts/` `../components/`，瘦入口使用 `@m3e/pages/HomePage.astro`
- astro build 通过，8 个页面全部正常生成
- 架构达成：packages/framework 持有全部可复用页面模板，apps/garden 仅保留路由入口 + app 专属配置

## FSD 分层重构（widgets + entities + shared 层）
- 目标：将 packages/framework/src/ 按 Feature-Sliced Design 分层
- 新建目录结构：
  - `widgets/hero/` — Hero 命名空间（greeting.astro + featured.astro）
  - `widgets/about/` — About 页面区块（bio.astro + m3-showcase.astro）
  - `widgets/header/` — Header widget（液态玻璃导航栏）
  - `widgets/footer/` — Footer widget
  - `widgets/recent-posts/` — 最近文章网格区块
  - `entities/post/` — 业务实体层（BlogCard.astro）
  - `shared/` — 基础设施层（components/ + config/ + i18n/ + types.ts + utils/ + styles/）
- 新建 widget 文件（7个）：
  - `widgets/hero/greeting.astro` — Hero 问候语（可扩展命名空间）
  - `widgets/hero/featured.astro` — Hero 精选文章（从 HomePage 提取，含 editorial 布局 + fallback）
  - `widgets/about/bio.astro` — 头像 + 双语 Bio
  - `widgets/about/m3-showcase.astro` — M3 组件展示汇总（Chip/Button/TextField/Surface/PullQuote）
  - `widgets/recent-posts/RecentPosts.astro` — 最近文章网格
  - `widgets/header/Header.astro` — 液态玻璃导航栏 widget
  - `widgets/footer/Footer.astro` — 页脚 widget
- `shared/` 层迁移：
  - `components/` → `shared/components/`（M3 UI 原语：Icon, I18n, MDBadge, MDButton, MDSnackbar, MDSwitch, Chip, PullQuote, TextField, ThemeFlash, MonetCSS, MonetSwitcher, Music, MusicPlayer, TableOfContents, FilterGroup, SearchBar, ThemeToggle, LanguageToggle, Backlinks）
  - `config/`, `i18n/`, `utils/`, `types.ts`, `styles/` → `shared/`（基础设施）
  - 删除旧 `components/` 目录
- `entities/` 层：创建 `entities/post/`，迁移 `BlogCard.astro`
- 重写页面为瘦入口（Task #14）：
  - `pages/HomePage.astro`（139→26 行）：import HeroGreeting + HeroFeatured + RecentPosts → 组装
  - `pages/AboutPage.astro`（287→23 行）：import AboutBio + M3Showcase → 组装
- import 路径全局修正：
  - `layouts/*.astro`：`../components/` → `../shared/components/`（M3 原语）+ `../widgets/`（Header/Footer）
  - `pages/*.astro`（apps/garden）：`@m3e/components/` → `@m3e/shared/components/` + `@m3e/entities/` + `@m3e/widgets/`
  - `entities/post/BlogCard.astro`：`../config/` → `../../shared/config/`
  - `widgets/*.astro`：`../../shared/components/`（M3 原语引用）
- `apps/garden/tsconfig.json` 新增 FSD 路径别名：
  - `@m3e/shared/*` → `packages/framework/src/shared/*`
  - `@m3e/widgets/*` → `packages/framework/src/widgets/*`
  - `@m3e/entities/*` → `packages/framework/src/entities/*`
  - `@m3e/pages/*` → `packages/framework/src/pages/*`
  - `@m3e/layouts/*` → `packages/framework/src/layouts/*`
- 构建验证通过（Task #16）：8 个页面全部正常生成，零错误
- 架构现状：
  - `shared/` — 基础设施（UI 原语 + 配置 + 类型 + 工具 + 样式 Token）
  - `entities/post/` — 业务实体（BlogCard）
  - `widgets/` — 大型 UI 组合块（Header, Footer, HeroFeatured, RecentPosts, AboutBio, M3Showcase）
  - `pages/` — 页面组装层（瘦入口，只做 data fetching + 组合）
  - `layouts/` — 应用配置层（BaseLayout, HeroLayout, PageLayout, SplitLayout, BlogListLayout, BlogPostLayout, WideLayout）
  - 依赖方向：pages → widgets → entities → shared（只能向下依赖，不能反向）

## 移除 HeroGreeting 组件引入
- 从 `packages/framework/src/pages/HomePage.astro` 移除 `HeroGreeting` 组件的引入
- 移除内容：
  - 删除 `import HeroGreeting from "../widgets/hero/greeting.astro";`
  - 删除 `<HeroGreeting slot="hero" siteName={siteName} />`
- 构建验证通过：8 个页面全部正常生成，零错误
- `widgets/hero/greeting.astro` 文件保留，以后可随时重新引入
