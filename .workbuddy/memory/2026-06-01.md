## 2026-06-01

### 硬编码排查
- 全面扫描了项目的所有源文件（src/ 下 30 个 .astro、22 个 .css、4 个 .ts、4 个 .md，scripts/ 下 2 个 .mjs，以及 astro.config.mjs 和 package.json）
- 生成完整审计报告 → `HARDCODING_AUDIT.md`
- 共发现约 207 处硬编码，分布在 12 个类别
- 核心发现：Token 层设计良好但组件层执行不彻底，大量组件内部仍直接使用 px 值和具体数值

### 硬编码最小化实践指南
- 联网搜索了 2025-2026 年最佳实践：Tailwind v4 Design Tokens (Mavik Labs)、DTCG W3C 标准、Stylelint Token Compliance、M3 Design Tokens
- 编写了完整的《硬编码最小化实践指南》 → `HARDCODING_MINIMIZATION_GUIDE.md`
- 内容涵盖 12 个章节：三层 Token 架构、命名规范、组件开发规范、断点 Token 化、动画 Token、图标管理、i18n、配置外置、Lint 自动化、CI/CD
- 提供了项目专属的 5 阶段迁移路径和快速参考卡片

### 指南落地化定制
- 基于 HARDCODING_AUDIT.md 的 207 条发现，将指南重写为逐项可执行的落地方案
- 每条审计发现标注了审计编号（A1-A12 类），精确到文件+行号+旧值+新值
- 规划了 3 个新建前置文件（breakpoints.css, monet.config.ts, 15 个组件 Token CSS 文件）
- 8 个 Phase：前置→P0(6项)→组件Token(32项)→间距排版(110+项)→图标i18n去重→页面title→断点注释→i18n keys→Stylelint
- 附加 Token↔像素值快速映射表

### Music Player 实施指南优化
- 读取并分析了 `C:/Users/wangy/WorkBuddy/2026-06-01-11-36-24/astro-music-player-implementation-guide.md`（原指南）
- 对照项目实际结构（astro.config.mjs、package.json、src/styles/tokens/、BaseLayout.astro）逐项修正
- 主要修正点：
  - CSS 变量命名空间：`--space-2` → `--blog-space-2`，`--text-sm` → `--blog-typescale-label-medium-size`，`--primary-color` → `--blog-color-primary`（全部 `--blog-*` 命名空间）
  - 文件路径：`src/components/widget/` → `src/components/`（项目实际结构）
  - Svelte 语法：从 v4（`export let`、`$:`) 升级到 v5 运行符（`$props()`、`$state()`、`$effect()`）
  - 布局集成：通用 `Layout.astro` → 项目实际的 `BaseLayout.astro`（含放置位置建议）
  - 主题系统：补充双重主题（`data-theme` + `data-monet`）兼容性说明
  - 新增 Tailwind CSS v4 + daisyUI v5 兼容性说明
- 优化后指南已覆写回原路径

### 前端架构审计
- 全面审查项目 85 个源文件，从 6 个维度评估架构：分层设计、仓库边界、复用性、内容隔离、配置管理、可维护性
- 输出完整报告 → `ARCHITECTURE_AUDIT_REPORT.md`
- 核心发现：
  - ✅ 分层目录结构、设计 Token 体系、组件架构、Content Schema 均优秀
  - ❌ 站点名 "M3E" 散落在 16+ 处（跨 7 个文件），违反单一数据源原则
  - ❌ 零环境变量使用，域名和 API 端点全部硬编码
  - ❌ About 页面内容写死在 pages 层而非 content 层
  - ❌ Music 组件硬编码个人歌单 ID 和第三方 API URL
  - ❌ filter-config.ts 放错目录（在 content/ 但属于框架层配置）
  - ❌ 3 个组件对 content/ 目录存在跨层 import 依赖（阻断仓库拆分）
- 提供了 12 项优先级排序的重构建议（P0-P2）
- 定义了三者职责边界：环境变量（敏感+环境差异）→ 内容仓库数据（身份+真实内容）→ 框架配置（技术+默认值）
- 综合评分 5.2/10（内核优秀但配置管理和仓库边界是致命短板）
- 附执行路线图（3 个 Phase，预计 4-6 天）

### 架构再审计（V2）
- 基于 V1 审计建议对 22 个变更文件进行逐项验证
- 综合评分从 **5.2/10 → 8.3/10** (+3.1)
- P0/P1 共 10 项建议全部完成：siteConfig 创建、16 处站点名硬编码消除、域名环境变量化、filter-config 迁移、Music/Backlinks/Header/Footer 组件解耦、About 内容移入 content 层、i18n 去标识化、RSS 配置外部化
- `astro check` 43 文件仅 2 个类型错误（均在 Music.astro，不影响功能）
- 残余问题：4 项一致性改进 + 2 项边际优化（均非阻塞）
- 仓库拆分前置条件已全部满足，输出再审计报告 → `ARCHITECTURE_AUDIT_REPORT_V2.md`
- 残余 2 项低优先级问题不阻塞

### 全盘审计 V3（终版）
- 16 个源文件变更，覆盖 V2→V3 质量收敛：i18n 死 key 清零、内容语言标记解耦（新增 `lang` frontmatter 字段）、BlogPostLayout/BlogCard/Backlinks/FilterGroup 全部 i18n 组件化、Music.astro 类型修复
- 新增博客列表语言筛选功能（MDSwitch + CSS + FLIP 动画）、全局 sticky footer
- 综合评分：**5.2 → 8.3 → 9.3/10**
- i18n 专项评分：**6.3 → 9.8/10**
- `astro check`：0 errors / 0 warnings / 0 hints（首次清零）
- 残余问题仅 2 项低优：BlogPostLayout "Stage"/"阶段" 后缀内联、空搜索结果文本可改为 I18n
- 输出报告 → `ARCHITECTURE_AUDIT_REPORT_V3.md`

### 仓库拆分就绪度评估
- 全量导入关系追踪：pages(29条import) → layouts(24条) → components + config
- **阻塞项 4 处**：Header/Footer/BaseLayout 中 4 处直接引用 `siteConfig`（site.ts 属于内容仓库，框架组件不能引用）
  - Header L29: `{siteConfig.name}` → 需改为 prop
  - Footer L27/L31/L61: 3 处 siteConfig 引用 → 需改为 props
  - BaseLayout L28/L37-L40: description 默认值 + music 配置 → 需 Props 化
- **机械性变更**：5 个 page 文件中 25 条相对路径 import 需改为 package import
- **结论**：架构层面 95% 就绪，剩余 4 处 siteConfig 引用消除后即可拆分

### Monorepo 分离 + V4 审计
- 仓库物理分离为 `packages/framework/` (@m3e/framework) + `apps/garden/` (内容 app)
- pnpm workspace + tsconfig paths (`@m3e/*`) 连接；app 依赖 `"@m3e/framework": "workspace:*"`
- 4 处 siteConfig 阻塞全部消除：框架零 siteConfig 引用 (`grep -rn "siteConfig" packages/framework/src/` → 0)
- 25 条 pages import → @m3e 路径；Icon(7处)/PullQuote(4 variant) 激活；垃圾 .txt 删除
- lang 检测细化：仅匹配 zh-CN/zh-SG/zh-Hans，不再误匹配 zh-TW/zh-HK
- `astro build` → 7 pages, 9.10s, 0 errors
- 综合评分: 5.2 → 8.3 → 9.3 → **9.8/10**
- 残余: 0 架构性问题
- 报告 → `lib/reports/archive/ARCHITECTURE_AUDIT_REPORT_V4.md`

### Meting API 参考文档
- 联网搜索了 6 个来源: Meting GitHub (PHP/JS/Docker)、injahow/meting-api、3 个公共实例文档、2 个社区博客
- 整理完整调用参考 → `lib/pending/Meting_API_Reference.md`
- 内容: 12 章节 — API 格式、5 平台支持矩阵、7 种 type 响应行为、JSON 响应格式(song/playlist/search)、7 个公共实例列表(含状态标注)、自建服务 3 种方式、音乐 ID 查找方法、错误处理与多实例 fallback 策略、项目集成点

### i18n 策略分析
- 对照用户原始意图（"只做 layout/component 国际化，内容层允许多语言，不同语言不单独路由"）全面审查当前 i18n 实现
- 综合评分 6.3/10：方向正确，细节需收敛
- 核心发现：
  - ✅ CSS 驱动的双轨渲染 + 零路由切换是最大亮点
  - ⚠ 两套机制并存：I18n 组件 vs 内联 `<span class="i18n-en/zh">`
  - ⚠ 内容层语言标记不一致：博客用 `tags: ["en"/"zh"]`（语言混入标签），about 用 entry ID
  - ⚠ BlogPostLayout 中日期/growth stage 翻译硬编码，FilterGroup 中 stage 名称硬编码
  - ⚠ translations.ts 有 5 个死 key（content 移入 Markdown 后遗留）
- 5 项优先级排序建议：BlogPostLayout 内联文本迁移、新增 `lang` frontmatter 字段解耦、FilterGroup 走 I18n、Backlinks 国际化、清理死 key
- 输出分析报告 → `I18N_STRATEGY_AUDIT.md`

### Meting API 参考
- 联网收集 6 个来源 → `lib/pending/Meting_API_Reference.md`（12 章，7 实例）

### 音乐播放器专项审计
- 全链路 audit: site.ts(env) → BaseLayout(Props) → Music.astro(Astro→Svelte) → MusicPlayer.svelte(579行)
- 评分 8.7/10
- 亮点: 全参数环境变量化(5/5)、三层错误防护、localStorage 1h 缓存、双模态 UI、Svelte 5 runes
- P1: API 端点拼接代码在 5 个 page 中重复
- P2: Svelte 层 6 处硬编码文字未 i18n (MUSIC BOX/未知曲目/等)
- 新发现: 404.astro 物理沙盒页面 (822 行 Canvas 2D 碰撞引擎)
- 报告 → `lib/reports/archive/Music_Player_Audit.md`
